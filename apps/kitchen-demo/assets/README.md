# Kitchen Demo Assets

These files are checked in so the media demo works without Telegram file ids.

- `sample-photo.jpg` is used by `send.photo`. Source: MDN interactive examples, CC0 image.
- `sample-video.mp4` is used by `send.video`. Source: MDN interactive examples, CC0 video.
- `sample-document.pdf` is used by `send.document`. Source: W3C dummy PDF test file.
